<Page x:Class="DiplomWork.StationAndPoints"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Controls.Validation;assembly=Controls"
      mc:Ignorable="d" 
	Title="StationAndPoints"> 
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,/Controls;component/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Grid Name="gr">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Click="AddRowOnClick">Добавить ряд</Button>
            <Button Click="AddColOnClick">Добавить колонку</Button>
        </StackPanel>
        <DataGrid Grid.Row="1" Name="mainGrid" AutoGenerateColumns="False" CanUserAddRows="False"  CanUserReorderColumns="False" CanUserSortColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название"
                                    Binding="{Binding Station.Name}"
                                    Width="100"
                                    CanUserSort="False"
                                    CanUserReorder="False"
                                    />
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid Grid.Row="2" Name="pointGrid"  AutoGenerateColumns="False" CanUserAddRows="False" CanUserReorderColumns="False" HeadersVisibility="None">
            <DataGrid.Columns>
                <DataGridTextColumn Width="100" IsReadOnly="True"
                                    CanUserSort="False"
                                    CanUserReorder="False">
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button Click="NextClick" Grid.Row="3" HorizontalAlignment="Right" Content="Далее">
            <!--<Button.Style>
                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource  {x:Type Button}}">
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type DataGridTextColumn}}, Path=(Validation.HasError)}" Value="false" />
                                <Condition Binding="{Binding ElementName=pointGrid, Path=(Validation.HasError)}" Value="false" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="IsEnabled" Value="False" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>-->
        </Button>
    </Grid>
</Page>
